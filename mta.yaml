_schema-version: "3.2"
ID: dealermangement
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: dealermangement-destination-content
  type: com.sap.application.content
  requires:
  - name: dealermangement-destination-service
    parameters:
      content-target: true
  - name: dealermangement-repo-host
    parameters:
      service-key:
        name: dealermangement-repo-host-key
  - name: dealermangement-uaa
    parameters:
      service-key:
        name: dealermangement-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: dealermangement_html_repo_host
          ServiceInstanceName: dealermangement-html5-service
          ServiceKeyName: dealermangement-repo-host-key
          sap.cloud.service: dealermangement
        - Authentication: OAuth2UserTokenExchange
          Name: dealermangement_uaa
          ServiceInstanceName: dealermangement-xsuaa-service
          ServiceKeyName: dealermangement-uaa-key
          sap.cloud.service: dealermangement
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: dealermangement-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: dealermangement-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - dealermangement.zip
      name: dealermangement
      target-path: resources/
- name: dealermangement
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: dealermangement-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: dealermangement-destination-service
    service-plan: lite
- name: dealermangement-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: dealermangement-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-name: dealermangement-xsuaa-service
    service-plan: application
- name: dealermangement-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: dealermangement-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
